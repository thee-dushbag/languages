fun fib(n) {
  if (n < 2) return 1;
  return fib(n - 1) + fib(n - 2);
}

var start = clock();
var f = fib(40);
var elapsed = clock() - start;
print "Time: " + string(elapsed) + " | fib(40): " + string(f);
